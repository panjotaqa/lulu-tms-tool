meta {
  name: Tentar cadastrar projeto com slug existente
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/projects
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Projeto Teste Slug Duplicado {{$timestamp}}",
    "slug": "{{existingSlug}}",
    "description": "Descrição do projeto com slug duplicado"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
  testEmail: test.project.slug@example.com
  testPassword: senhaSegura123
  existingSlug: projeto-slug-existente
}

script:pre-request {
  const email = bru.getVar("testEmail");
  const password = bru.getVar("testPassword");
  try {
    const createUserResponse = await bru.post("{{baseUrl}}/users", {
      body: {
        name: "Usuário Teste Slug",
        email: email,
        password: password
      },
      options: {
        headers: {
          "Content-Type": "application/json"
        }
      }
    });
    if (createUserResponse.status === 201 || createUserResponse.status === 409) {
      const loginResponse = await bru.post("{{baseUrl}}/auth/login", {
        body: {
          email: email,
          password: password
        },
        options: {
          headers: {
            "Content-Type": "application/json"
          }
        }
      });
      if (loginResponse.status === 200) {
        const loginBody = loginResponse.body;
        bru.setVar("token", loginBody.token);
        const createProjectResponse = await bru.post("{{baseUrl}}/projects", {
          body: {
            title: "Projeto Slug Existente",
            slug: bru.getVar("existingSlug"),
            description: "Projeto com slug existente"
          },
          options: {
            headers: {
              "Content-Type": "application/json",
              "Authorization": "Bearer " + loginBody.token
            }
          }
        });
      }
    }
  } catch (err) {
    console.error("Erro no pré-script:", err);
  }
}

tests {
  test("deve retornar erro ao tentar cadastrar projeto com slug existente", () => {
    expect(res.getStatus()).to.equal(409);
    const responseBody = res.getBody();
    expect(responseBody).to.be.an('object');
    expect(responseBody).to.have.property('message');
    expect(responseBody.message).to.include('slug');
  });
}

