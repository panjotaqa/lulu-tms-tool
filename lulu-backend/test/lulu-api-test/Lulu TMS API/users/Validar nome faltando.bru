meta {
  name: Validar nome faltando
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "email": "joao.silva@example.com",
    "password": "senhaSegura123"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

tests {
  test("deve retornar 400 quando nome está faltando", () => {
    expect(res.getStatus()).to.equal(400);
  });
}

