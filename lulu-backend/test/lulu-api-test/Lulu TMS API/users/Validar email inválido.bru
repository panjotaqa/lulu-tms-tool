meta {
  name: Validar email inválido
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "João Silva",
    "email": "email-invalido",
    "password": "senhaSegura123"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

tests {
  test("deve retornar 400 quando email é inválido", () => {
    expect(res.getStatus()).to.equal(400);
  });
}

