services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: lulu-frontend-prod
    ports:
      - "8080:80"
    restart: always
    networks:
      - lulu-network

networks:
  lulu-network:
    driver: bridge